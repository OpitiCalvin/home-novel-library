services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: novellib_ui
    ports:
      - "3000:3000"
    env_file:
      - ./.env
    volumes:
      - ./public:/home/nextjs/app/public
      # - ./logs:/home/nextjs/app/logs